import { TooltipDirective } from './tooltip.directive';

describe.skip('TooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipDirective();
    expect(directive).toBeTruthy();
  });
});
